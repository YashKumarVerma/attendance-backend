const chai = require('chai')
const { suite, test, before } = require('mocha')

chai.should()

suite('Testing /user route', () => {
  before((done) => {
    done()
  })

  test('should create a new user', (done) => {
    done()
  })

  test('should login as a new user', (done) => {
    done()
  })
})
